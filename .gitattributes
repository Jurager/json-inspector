# Generated files are rewritten by the build, and the generators always emit LF.
# Pinning them keeps a Windows checkout byte-identical to the output, so git
# stops warning about CRLF conversion on every run.
frontend/bindings/** text eol=lf
