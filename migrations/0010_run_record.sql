-- Строка прогона называет запись, которую произвела: без этой ссылки строка умеет показать только
-- код ответа и время, а клик по ней открывает запрос без ответа — тогда как строка истории в том же
-- окне открывает свой. Колонка та же, что и у отчётов скриптов, и по той же причине: прогон должен
-- уметь рассказать, что именно он отправил.
--
-- У запроса, который скрипт не пустил, записи нет вообще — так и остаётся пусто.

ALTER TABLE collection_run_results ADD COLUMN record_id TEXT;
