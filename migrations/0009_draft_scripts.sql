-- Скрипты есть и у запроса, который ещё никто не сохранил: командная строка даёт написать код
-- вокруг отправки, и он живёт вместе с черновиком, а не вместе с коллекцией — у запроса без места в
-- дереве нет ни коллекции, ни узла, к которым его можно было бы приписать.
--
-- NULL значит то же, что и у уровней дерева: «здесь не задано» — у командной строки это «нечего
-- добавить», и заводить пустую строку вместо отсутствия значило бы записать «ничего» там, где
-- имеется в виду «и так сойдёт».

ALTER TABLE drafts ADD COLUMN scripts_json TEXT;
