version: '3'

vars:
  APP_NAME: "json-inspector"
  BIN_DIR: "bin"
  PACKAGE_MANAGER: '{{.PACKAGE_MANAGER | default "npm"}}'
  VITE_PORT: '{{.WAILS_VITE_PORT | default 9245}}'
  VERSION: '{{.VERSION | default "dev"}}'
  GOOS: '{{.GOOS | default OS}}'
  # Read back out of build/config.yml, which feeds the Info.plist, the NSIS
  # script and the nfpm package — so the product metadata has one source.
  PRODUCT_NAME:
    sh: sed -n 's/^  productName:[[:space:]]*"\?\([^"]*\)"\?[[:space:]]*$/\1/p' build/config.yml
  PRODUCT_DESCRIPTION:
    sh: sed -n 's/^  description:[[:space:]]*"\?\([^"]*\)"\?[[:space:]]*$/\1/p' build/config.yml

includes:
  common: ./build/Taskfile.yml
  windows: ./build/windows/Taskfile.yml
  darwin: ./build/darwin/Taskfile.yml
  linux: ./build/linux/Taskfile.yml

tasks:
  build:
    summary: Builds the application
    cmds:
      - task: "{{.GOOS}}:build"

  package:
    summary: Packages a production build of the application
    cmds:
      - task: "{{.GOOS}}:package"

  run:
    summary: Runs the application
    cmds:
      - task: "{{.GOOS}}:run"

  dev:
    summary: Runs the application in development mode
    cmds:
      - wails3 dev -config ./build/config.yml -port {{.VITE_PORT}}
