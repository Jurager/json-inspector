// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * DraftService is the request the window is composing. The window draws it and asks for changes;
 * what those changes mean — which rows a URL has, what a `{{token}}` resolves to, whether the
 * request can go out — is decided here.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as domain$0 from "../../domain/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as draft$0 from "../../usecase/draft/models.js";

export function AddRow(kind: domain$0.RowKind): $CancellablePromise<draft$0.State> {
    return $Call.ByID(1330992221, kind);
}

export function PatchRow(kind: domain$0.RowKind, id: string, patch: draft$0.RowPatch): $CancellablePromise<draft$0.State> {
    return $Call.ByID(816954078, kind, id, patch);
}

/**
 * RemoveRow and PatchRow address a row by id and not by position: a click that lands after the list
 * changed under it must not delete the row that took its place.
 */
export function RemoveRow(kind: domain$0.RowKind, id: string): $CancellablePromise<draft$0.State> {
    return $Call.ByID(4249851392, kind, id);
}

/**
 * Replace hands the draft a whole request: "открыть в запросе" on a record, or a command pasted
 * into the command line.
 */
export function Replace(seed: draft$0.Seed): $CancellablePromise<draft$0.State> {
    return $Call.ByID(2303442316, seed);
}

export function SetAuth(auth: domain$0.Auth): $CancellablePromise<draft$0.State> {
    return $Call.ByID(3062809258, auth);
}

export function SetMethod(method: string): $CancellablePromise<draft$0.State> {
    return $Call.ByID(2905225717, method);
}

/**
 * SetText is a buffer flush: the URL and the body are the window's while they are being typed, and
 * this is how they reach this side. The revision travels back with the answer so the window can
 * tell a reply to the keystroke it just made from one to the keystroke before it.
 */
export function SetText($in: draft$0.TextInput): $CancellablePromise<draft$0.TextResult> {
    return $Call.ByID(819678171, $in);
}

/**
 * Snapshot is the draft as it stands, which is what the window opens on.
 */
export function Snapshot(): $CancellablePromise<draft$0.State> {
    return $Call.ByID(2263784320);
}
