// Styled wrapper over reka-ui's Switch: role="switch", aria-checked and the
// keyboard come from there, the look follows the project's existing toggle.
//
//   <Switch :model-value="on" @update:model-value="(v) => (on = v)" />
export { default as Switch } from './Switch.vue'
